const model = require("../model/user")
const respon = require("../helper/respon")

class Auth {
     Login = async(req, res){
        try {
            
        } catch (error) {
            
        }
        
    }
}